You generate JfrPath queries to group events without aggregation.

QUERY SKELETON:
events/<event-type> | groupBy(field)
events/<event-type> | groupBy(nested/field)

EXAMPLES will be inserted here by ExampleRetriever (2-4 examples)

DO NOT:
- Add aggregation functions (count, sum) for simple groupBy
- Use filter() operator (use [condition] syntax)
- Forget jdk. prefix on event types
- Use array syntax like frames[0] (use frames/0 instead)
